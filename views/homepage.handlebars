<div id="all-posts">
{{#each posts as |post|}}
<div class="card">
    <div class="card-body">
        <h4 class="card-title">{{post.title}}</h4>
        <p class="card-text">{{post.content}}</p>
        <p class="small-text">written by: {{post.user.username}}</p>
        <p class="small-text">on: {{format_date post.date_created}}</p>
        <a href="/post/{{post.id}}" class="btn btn-primary">See Full Post</a>
        
    </div>
</div>
{{/each}}
</div>

<script src="./js/logout.js"></script>